# Shiva Gaddam Portfolio

Minimal Next.js portfolio scaffold for v1.

## Structure

- `app/` for routing and global layout
- `components/` for reusable UI and sections
- `content/` for all portfolio copy and data
- `config/` for theme configuration
- `styles/` for CSS variables and typography rules
- `lib/` for shared utilities

